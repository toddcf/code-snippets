// Ternary Operator:
activePlayer === 0 ? activePlayer = 1 : activePlayer = 0;

/*
In plain English:
- IF activePlayer strictly equals zero, THEN set activePlayer to 1.
- OTHERWISE, set activePlayer to zero.