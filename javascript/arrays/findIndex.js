// findIndex, new for ES6:

// Example:
var ages = [ 12, 17, 8, 21, 14, 11 ];
// Return the index of the cur element if that element is greater than 18:
console.log( ages.findIndex( cur => cur >= 18 ) );