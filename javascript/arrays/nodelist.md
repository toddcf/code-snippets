# Nodelist

A nodelist is similar to an array, but not identical.

Using `querySelectorAll` to retrieve multiple elements on a page will return a nodelist, for example.


## The Differences Between a Nodelist and an Array

A nodelist does not have all the methods attached to it that an array would have, such as map, filter, reduce, etc.


## Converting a Nodelist to an Array