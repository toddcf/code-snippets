# Z-Index

## Explanation

The elements with the highest z-indexes appear first. Lower z-indexes are pushed to the background.

## Example

`z-index: 10;` would be in the foreground compared to `z-index: 0;`, which would be in the background.